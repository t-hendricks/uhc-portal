/**
 * Browser tab title prefixes for install pages (AppPage / appPageTitle).
 * Version-neutral: these pages serve both 4.x and 5.x.
 */

/** Pattern A: Install OpenShift | Red Hat OpenShift Cluster Manager | {Provider} */
export const INSTALL_PAGE_PREFIX = 'Install OpenShift | Red Hat OpenShift Cluster Manager';

/** Pattern B: Install OpenShift | {Suffix} (no RH OCM middle segment) */
export const INSTALL_OPENSHIFT_PREFIX = 'Install OpenShift';
